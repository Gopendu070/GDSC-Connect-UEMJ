import 'package:flutter/material.dart';

class AdminEdittingScreen extends StatelessWidget {
  const AdminEdittingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Admin Controls"),
      ),
      body: Container(),
    );
  }
}
